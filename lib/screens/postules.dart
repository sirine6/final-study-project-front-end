import 'package:flutter/material.dart';


class Postules extends StatelessWidget {
  const Postules({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(),

      body: Center(
        child: Text("Postules"),
      ),
      
    );
  }
}